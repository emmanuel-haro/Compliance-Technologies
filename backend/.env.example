# MongoDB connection string
MONGO_URI=mongodb+srv://<user>:<password>@cluster0.mongodb.net/compliancetech?retryWrites=true&w=majority

# Backend server port
PORT=5000

# Frontend origin allowed for CORS
FRONTEND_ORIGIN=http://localhost:8080

# SMTP configuration (for sending contact emails)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false # true for 465
SMTP_USER=your_smtp_user@example.com
SMTP_PASS=your_smtp_password

# Company email to receive contact submissions
COMPANY_EMAIL=info@compliancetech.co.ke